---
description: Run lint and TypeScript checks and report failures
---

Run both checks and report the results:

```bash
npm run lint
npx tsc --noEmit
```

Show the actual error output for anything that fails — don't summarize it away.
If both pass, say so in one line. Do not fix anything unless I ask.
