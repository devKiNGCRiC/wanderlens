---
description: Look up the SDK 54 docs for an Expo module before using it
argument-hint: [module name, e.g. expo-image-picker]
---

Fetch the Expo SDK 54 documentation for: $ARGUMENTS

Start at https://docs.expo.dev/versions/v54.0.0/ and find that module's page.

Report:
- The current SDK 54 API signature
- Anything that changed from earlier SDKs and would break recalled usage
- The minimal correct example for this project

Do not answer from memory. If the page doesn't confirm something, say it's
unconfirmed rather than filling the gap.
